class Solution {
public:
        bool isBalanced(TreeNode* root) {
                TreeNode *lroot = root->left;
                TreeNode *rroot = root->right;

                return true; // 保持测试完整性，记得删除
        }
};


int main(void)
{


        return 0;
}
